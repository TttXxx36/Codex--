# Codex++ 第二轮开发演进大纲与实施跟踪 (DEVELOPMENT_PLAN_2)

> **项目目标**：打造极致轻量、超低 CPU 占用、无广告侵扰、零端口死锁、高可靠的工业级 Codex 桌面增强与管理套件。  
> **文档定位**：全量承接第一阶段大纲 `DEVELOPMENT_PLAN.md` 任务 01～15 成果（v1.2.58 稳定里程碑），作为第二轮深度稳健重构与可靠性自愈的全局执行主轴。  
> **当前基线**：Commit `dc9ae8b` (`v1.2.58`)，分支 `Gemini`。  
> **当前测试套件**：`apps/codex-plus-manager/` 目录下 `npm test`，共 **187 项自动化单测全部通过（0 failure）**。  
> **存放位置**：项目根目录 `DEVELOPMENT_PLAN_2.md`（实时更新维护）。

---

## 📌 开发大纲维护机制与执行原则

1. **结构传承与逻辑对齐**：
   - 严格继承第一阶段大纲的架构框架与标准范式，保持逻辑与工程结构 100% 一致性；
   - 每个任务节点均严格落实三要素闭环跟踪：
     - 🎯 **每一步的计划 (Plan)**：明确功能边界、解决的核心痛点与技术路线；
     - 🛠️ **实际完成的步骤 (Actual Steps)**：记录代码修改的具体文件、核心逻辑改造与安全处理；
     - ✅ **实际完成的结果 (Results & Verification)**：提供客观的验证指标、测试结果与前后对比。
2. **严谨的真实性与证据等级判定 (True Grounding)**：
   - 严格区分“纸面宣称/理论指标”与“实测数据”，所有性能指标实测前标注为目标值，禁止无证据断言；
   - 严格区分四级证据：**S**（源码存在）/ **T**（单元测试通过）/ **C**（CI/构建产物生成）/ **D**（桌面端端到端运行验证）；
   - 告别纯前端内存模拟，涉及 `config.toml`、`auth.json`、环境变量或端口治理时，必须由后端拥有真实的恢复语义与状态。
3. **渐进式实施铁律（稳定性与门禁优先）**：
   - **第一阶段（P0 深度稳定性与凭据事务闭环）**：优先攻坚供应商切换后端真实事务恢复、环境变量归属与 401 契约、Launcher/Bridge 端口与生命周期自愈；
   - **第二阶段（P1 隐私路径收口与质量门禁强守护）**：收口本地 Markdown 分享语义，建立 CI 最小自动化门禁（Node/TypeScript/Vite/Rust）；
   - **第三阶段（P2 架构解耦与海量数据性能拓展）**：在门禁守护下安全推进 App.tsx 模块级动态路由拆分、renderer 模块打包、SQLite 真实 Keyset 接入与协议深度适配。
4. **安全底线与凭据保护**：
   - 严禁真实凭据、Token、Cookie 或未脱敏数据进入日志、快照、测试用例或 Git 历史。

---

## 🔍 第二轮核心问题与架构共识总结

在对第一轮 15 个任务实施成果与系统深层潜在缺陷进行交叉复核后，确立以下第二轮核心攻坚事实：

1. **供应商切换跨层事务缺口**：
   - 第一阶段任务 14 实现了前端差异预检与 React 内存撤销，但底层 `config.toml` 与 `auth.json` 仍为单向直接覆盖，撤销 ID 由前端生成，缺乏后端拥有的不可伪造恢复句柄；撤销若在后端失败，前端不应误报成功。
2. **环境变量判定模型需要从“名称前缀”升级为“归属与值级契约”**：
   - 避免粗暴按 `OPENAI_` 前缀清理，防止误删用户自定义系统的其它合法 key；只有环境变量真实属于历史冲突 profile 且与预期不一致时才提示处理，消除客户端启动后的潜在 401 鉴权故障。
3. **Launcher 守护状态机需由“无限轮询”进化为“有限退避与明确终态”**：
   - 必须精确区分进程退出、CDP 离线、Bridge stale 与端口占用，禁止在动态端口路径硬编码固定 57321，进程彻底退出后必须在有限窗口内停止轮询，杜绝生成重试风暴。
4. **大规模解耦重构必须以前置门禁为依托**：
   - 1.13 万行 `App.tsx` 与 1.05 万行 `renderer-inject.js` 的模块化拆分应放在核心稳定闸门之后，并由严格的类型检查和构建测试流水线提供兜底。

---

## 🗺️ 第二轮任务演进全景图 (Mermaid)

```mermaid
graph TD
    subgraph 基线继承阶段
        V1["✅ 阶段一里程碑: v1.2.58 (任务 01 ~ 15 全部交付并发布)"]
    end

    subgraph 第一阶段：深度稳定性与凭据事务闭环 (P0)
        T16["🎯 任务 16 (BUG-008): 供应商切换后端跨层事务与原子快照撤销"]
        T17["🎯 任务 17 (BUG-009): 环境变量值级归属判定与 401 凭证防破坏"]
        T18["🎯 任务 18 (BUG-010): Launcher / Bridge 真实进程存活与端口状态机自愈"]
    end

    subgraph 第二阶段：隐私收口与测试门禁强守护 (P1)
        T19["🎯 任务 19 (BUG-012): 纯本地分享语义彻底收敛与残留外联清理"]
        T20["🎯 任务 20 (TEST-001): 跨平台最小自动化 E2E 回归与 CI 质量门禁"]
    end

    subgraph 第三阶段：架构解耦、海量数据与协议深度拓展 (P2)
        T21["🎯 任务 21 (PERF-005): App.tsx 1.13 万行真·路由文件拆分与 lazy chunk 编译"]
        T22["🎯 任务 22 (PERF-006): renderer-inject.js 1.05 万行单 IIFE 模块工程化打包"]
        T23["🎯 任务 23 (PERF-007): SQLite 多 Schema 真实接入与 Keyset 游标后端闭环"]
        T24["🎯 任务 24 (BUG-011): 国产大模型协议 / 多模态 / WeChat 权限枚举深度契约"]
    end

    V1 --> T16
    T16 --> T17
    T17 --> T18
    T18 --> T19
    T19 --> T20
    T20 --> T21
    T21 --> T22
    T22 --> T23
    T23 --> T24
```

---

## 📝 详细任务定义与实施计划

### 【任务 16 (P0 / BUG-008)】供应商切换后端跨层事务与原子快照撤销

- **🎯 阶段计划 (Plan)**：
  - **解决核心痛点**：
    1. 第一阶段任务 14 实现了前端预检与 React 内存撤销，但底层 `config.toml` 与 `auth.json` 为单向写入，撤销 ID 由前端生成，缺乏后端拥有的不可伪造事务快照；
    2. 切换中途若遇磁盘权限或进程崩溃，容易遗留半同步的损坏文件；且撤销调用若在后端失败，前端存在误报成功的异常路径。
  - **核心实施方案**：
    - 复用现有 `switch_relay_profile` 与 `LiveFilesSnapshot`，不新建过于冗余的外部重型数据库；
    - 后端在切换前对当前 live 的 `settings.json`、`config.toml`、`auth.json` 捕获原始快照并记录文件校验指纹；
    - 维持最近一次成功切换的可撤销实体（存放于本地隔离备份目录），提供 `restore_last_switch(token)` 真实回滚接口；
    - 回滚前校验当前文件指纹仍属于该次切换的生成版本，若已被用户外部手动修改则拒绝覆盖并报错；
    - 切换或撤销失败时返回 `recovery_required` 结构化状态，UI 必须展示真实失败信息与恢复指引，坚决阻断虚假“撤销成功”。
  - **必须覆盖的测试场景**：
    - `settings.json` 保存失败场景；
    - `auth.json` / `config.toml` 写入中断场景；
    - 切换成功后的一键还原往返测试；
    - 撤销时文件已被外部修改的防踩踏冲突拒绝。

- **🛠️ 实际完成的步骤 (Actual Steps)**：
  - *（待任务实施后登记具体文件修改、核心逻辑改造与提交 commit）*

- **✅ 实际完成的结果 (Results & Verification)**：
  - *（待任务验证后登记客观测试命令、通过指标与失败路径覆盖断言）*

---

### 【任务 17 (P0 / BUG-009)】环境变量值级归属判定与 401 凭证防破坏

- **🎯 阶段计划 (Plan)**：
  - **解决核心痛点**：
    1. 消除粗暴按 `OPENAI_` 前缀判定冲突的缺陷，防止误删用户自定义系统的其它合法环境变量；
    2. 解决检测成功但实际请求缺少 Authorization 头导致的 401 故障，建立系统环境、`auth.json` 与目标 profile 的值级一致性判定。
  - **核心实施方案**：
    - 明确区分进程环境变量 (process env) 与 Windows 用户注册表环境变量 (user env)；
    - 建立“变量名 + 来源 + 当前值哈希 + 归属状态”的多维判定实体，当前值与当前 profile 完全一致时自动判定为“一致”，不弹出误报；
    - 删除必须由用户明确选择，并携带预期指纹校验，实际值变化时拒绝删除；
    - 使用本地 fake server 校验 4 种供应商模式（官方登录/混入/纯API/聚合）下的 Authorization 请求头行为，不依赖真实公网请求。
  - **必须覆盖的测试场景**：
    - 相同密钥不重复弹窗误报测试；
    - 外部修改后的变量免误删保护测试；
    - 4 种模式下 fake server 鉴权请求头逐项断言；
    - 备份落盘与逆向一键还原往返一致性测试。

- **🛠️ 实际完成的步骤 (Actual Steps)**：
  - *（待任务实施后登记具体文件修改、核心逻辑改造与提交 commit）*

- **✅ 实际完成的结果 (Results & Verification)**：
  - *（待任务验证后登记客观测试命令、通过指标与失败路径覆盖断言）*

---

### 【任务 18 (P0 / BUG-010)】Launcher / Bridge 真实进程存活与端口状态机自愈

- **🎯 阶段计划 (Plan)**：
  - **解决核心痛点**：
    1. 解决 helper 端口在动态分配时部分代码路径仍硬编码 57321 的参数贯穿缺口；
    2. 解决 Watchdog 轮询缺乏终态退出机制、官方应用退出后仍盲目制造 generation 重试风暴的问题。
  - **核心实施方案**：
    - 规范化端口参数贯穿，禁止在动态端口分支中硬编码 57321；
    - 固化守护状态机：`starting` -> `running` -> `degraded` -> `stopping` -> `stopped` -> `needs_user_action`；
    - 精准解耦失败原因：区分“官方进程退出”、“CDP 端口不可达”、“Bridge 不健康”、“端口已被占用”四大类；
    - 设定重试时间与次数的有界上限，重试超时后主动停机并释放系统资源；同一 Bridge 禁止并发重注入。
  - **必须覆盖的测试场景**：
    - 端口占用、动态端口探测与失败退避单测；
    - 官方应用退出后守护进程有限重试并成功进入 `stopped` 测试；
    - 重复调用 stop / restart 不产生孤儿进程或任务泄漏。

- **🛠️ 实际完成的步骤 (Actual Steps)**：
  - *（待任务实施后登记具体文件修改、核心逻辑改造与提交 commit）*

- **✅ 实际完成的结果 (Results & Verification)**：
  - *（待任务验证后登记客观测试命令、通过指标与失败路径覆盖断言）*

---

### 【任务 19 (P1 / BUG-012)】纯本地分享语义彻底收敛与残留外联清理

- **🎯 阶段计划 (Plan)**：
  - **解决核心痛点**：
    1. 虽然第一阶段已阻断会话公网上传，但 `renderer-inject.js` 仍残留对远程 `share.codexpp.cc` 的常量引用与 `/share/create` 失效调用代码；
    2. 彻底理清“本地安全模式”边界，消除源码与测试中可能引起用户误解的远程分享痕迹。
  - **核心实施方案**：
    - 彻底从注入端脚本中移除远程分享 URL 拼接、无效加密远程参数及 Discord/Telegram 遗留点击代码；
    - 会话分享纯粹保留为本地结构化 Markdown 导出与系统剪贴板安全复制；
    - 文档与界面提示明确声明纯本地模式。
  - **必须覆盖的测试场景**：
    - 点击会话导出无任何外部网络请求测试；
    - 本地 Markdown 导出与还原往返测试；
    - 静态源码断言无远程分享伪链接构造。

- **🛠️ 实际完成的步骤 (Actual Steps)**：
  - *（待任务实施后登记具体文件修改、核心逻辑改造与提交 commit）*

- **✅ 实际完成的结果 (Results & Verification)**：
  - *（待任务验证后登记客观测试命令、通过指标与失败路径覆盖断言）*

---

### 【任务 20 (P1 / TEST-001)】跨平台最小自动化 E2E 回归与 CI 质量门禁

- **🎯 阶段计划 (Plan)**：
  - **解决核心痛点**：
    1. 为后续的大规模组件拆解与工程化构建建立绝对可靠的自动化质量门禁；
    2. 避免本地依赖缺失或环境漂移导致构建在云端静默失败。
  - **核心实施方案**：
    - 完善自动化测试矩阵，覆盖 Node 单测、TypeScript 类型检查 (`tsc --noEmit`)、Vite 构建 (`vite build`) 与 Rust 工作区单测 (`cargo test --workspace`)；
    - 固化目标选择、注入挂载、重启停机、弹性选择器的无网络 Smoke 测试集；
    - 在 PR 构建流中加入对假密钥、Bearer、Cookie 泄露的自动化静态脱敏扫描。
  - **必须覆盖的测试场景**：
    - Windows 环境下完整的四维 Smoke 测试；
    - 自动化流水线在发生任何类型错误或单测失败时能强力阻断合入。

- **🛠️ 实际完成的步骤 (Actual Steps)**：
  - *（待任务实施后登记具体文件修改、核心逻辑改造与提交 commit）*

- **✅ 实际完成的结果 (Results & Verification)**：
  - *（待任务验证后登记客观测试命令、通过指标与失败路径覆盖断言）*

---

### 【任务 21 (P2 / PERF-005)】App.tsx 1.13 万行真·路由文件拆分与 lazy chunk 编译

- **🎯 阶段计划 (Plan)**：
  - **前置依赖**：必须在任务 16～20 全部完成并通过 CI 质量门禁后实施。
  - **解决核心痛点**：
    - 当前 1.17 万行 `App.tsx` 所有 Screen 组件虽包裹在 `memo` 中，但仍物理堆叠在单一文件中，导致开发难以维护且编译产物过大。
  - **核心实施方案**：
    - 建立清晰的 `views/` 目录，单次逐个解耦 Screen 视图组件（如 `views/RelayScreen.tsx`、`views/SessionsScreen.tsx`）；
    - 采用模块顶层声明的 `lazy(() => import(...))` 进行异步按需加载，配合 `Suspense` 与 `ErrorBoundary`；
    - 将 `App.tsx` 瘦身至纯粹的顶层 Shell 容器与路由协调器（800 行以内）；
    - 保持所有状态、默认配置与操作逻辑 100% 不变。

- **🛠️ 实际完成的步骤 (Actual Steps)**：
  - *（待任务实施后登记具体文件修改、核心逻辑改造与提交 commit）*

- **✅ 实际完成的结果 (Results & Verification)**：
  - *（待任务验证后登记客观测试命令、通过指标与失败路径覆盖断言）*

---

### 【任务 22 (P2 / PERF-006)】renderer-inject.js 1.05 万行单 IIFE 模块工程化打包

- **🎯 阶段计划 (Plan)**：
  - **解决核心痛点**：
    - `renderer-inject.js` 目前为 10,500 行的原生混写大脚本，缺乏模块化与静态检查。
  - **核心实施方案**：
    - 将生命周期、选择器适配、会话处理、Markdown 导出、样式注入与 Bridge 通信解耦为独立模块；
    - 利用 Rollup/Vite 构建输出自包含单一 bundle，保持与 Rust 嵌入层 `assets.rs` 的完全透明兼容；
    - 增加模块级单元测试。

- **🛠️ 实际完成的步骤 (Actual Steps)**：
  - *（待任务实施后登记具体文件修改、核心逻辑改造与提交 commit）*

- **✅ 实际完成的结果 (Results & Verification)**：
  - *（待任务验证后登记客观测试命令、通过指标与失败路径覆盖断言）*

---

### 【任务 23 (P2 / PERF-007)】SQLite 多 Schema 真实接入与 Keyset 游标后端闭环

- **🎯 阶段计划 (Plan)**：
  - **解决核心痛点**：
    - 第一阶段任务 13 建立了预排序覆盖索引与前端辅助函数，但真实列表调用链仍主要依赖传统 `offset` 分页，未真正打通 Keyset 游标后端穿透。
  - **核心实施方案**：
    - 保留 offset 作为安全兼容回退；
    - 实现后端真正的 Keyset 游标查询接口，前端滚动时以 `(updated_at_ms, id)` 游标向前步进；
    - 兼容只读数据库与各种历史 Codex 数据库版本，建索引失败时优雅降级。

- **🛠️ 实际完成的步骤 (Actual Steps)**：
  - *（待任务实施后登记具体文件修改、核心逻辑改造与提交 commit）*

- **✅ 实际完成的结果 (Results & Verification)**：
  - *（待任务验证后登记客观测试命令、通过指标与失败路径覆盖断言）*

---

### 【任务 24 (P2 / BUG-011)】国产大模型协议 / 多模态 / WeChat 权限枚举深度契约

- **🎯 阶段计划 (Plan)**：
  - **解决核心痛点**：
    - 针对第三方大模型与微信接入中的特定兼容性差异进行精准修复。
  - **核心实施方案**：
    - 修正 WeChat app-server 调用的 sandbox 枚举映射（如 `read-only` -> `readOnly`，`workspace-write` -> `workspaceWrite`，`danger-full-access` -> `dangerFullAccess`）；
    - 细化模型能力矩阵，对不支持 custom tool 的模型实现标准 function 转换或安全降级；
    - 针对国产模型流式思考链 `<think>` 标签进行标准化契约守护。

- **🛠️ 实际完成的步骤 (Actual Steps)**：
  - *（待任务实施后登记具体文件修改、核心逻辑改造与提交 commit）*

- **✅ 实际完成的结果 (Results & Verification)**：
  - *（待任务验证后登记客观测试命令、通过指标与失败路径覆盖断言）*

---

## 🛑 跨任务数据安全准则与终止条件

1. **凭据安全绝对红线**：
   - 任何涉及 API Key、Cookie、Bearer 或用户私密会话的内容，绝不允许出现在测试 fixture、日志输出或提交历史中；
2. **操作原子性与双向回滚**：
   - 涉及系统注册表、环境变量或配置文件写入时，必须具备原子快照与失败补偿；
3. **遇到以下情况必须立即停止并保留现场**：
   - 切换可能破坏或覆盖用户当前有效凭据；
   - 撤销动作无法被后端有效验证；
   - 进程或端口出现无法自愈的死锁状态。
