# Codex++ 第二轮开发演进大纲与实施跟踪 (DEVELOPMENT_PLAN_2)

> **文档定位**：全量承接根目录 `DEVELOPMENT_PLAN.md` 任务 01～15 成果（v1.2.58 稳定里程碑），作为第二轮深度重构与性能自愈的全局主轴。  
> **当前基线**：Commit `dc9ae8b` (`v1.2.58`)，分支 `Gemini`。  
> **当前验证指标**：`apps/codex-plus-manager` 单测 **187 项全部绿灯通过（0 failure）**。  
> **核心原则**：不把“前端 UI 模拟”等同于“后端真实事务”；不把“单测通过”等同于“桌面生命周期闭环”；严防凭据丢失、死锁与不可逆覆盖。

---

## 📌 第二轮开发原则与总体目标

1. **真实性与跨层一致性 (True Grounding)**：
   - 告别纯前端内存 state 撤销，所有涉及 `config.toml`、`auth.json`、系统注册表与环境变量的动作，必须具备 Rust 后端不可变事务句柄（Transaction Handle）与磁盘快照回滚能力。
2. **渐进式稳健推进路线**：
   - **第一阶段（P0 深度稳定性与凭据事务闭环）**：攻坚供应商切换跨层真实事务与撤销、环境变量/认证归属契约、Launcher/Bridge 进程与端口生命周期状态机。
   - **第二阶段（P1 架构解耦与工程化治理）**：将 1.13 万行 `App.tsx` 真正按路由文件拆分为 `views/` 模块级动态加载，对 1.05 万行 `renderer-inject.js` 进行工程化拆解与模块打包。
   - **第三阶段（P2 数据性能、协议深度兼容与可观测性）**：SQLite 真实 Schema 覆盖与 Keyset 接入、多模态与国产协议契约扩展、端到端自动化 Smoke 闭环。
3. **闭环跟踪规范 (Plan -> Actual Steps -> Results & Verification)**：
   - 每一个任务均严格记录代码改动、测试断言与客观验证数据，严禁虚构未达成的性能指标。

---

## 🗺️ 第二轮任务演进全景图 (Mermaid)

```mermaid
graph TD
    subgraph 基线继承阶段
        V1["✅ 阶段一里程碑: v1.2.58 (任务 01 ~ 15 全部完成并发布)"]
    end

    subgraph 第一阶段：深度稳定性与凭据事务闭环 (P0)
        T16["🎯 任务 16 (BUG-008): 供应商切换后端跨层事务与原子快照撤销"]
        T17["🎯 任务 17 (BUG-009): 环境变量值级归属判定与 401 凭证防破坏"]
        T18["🎯 任务 18 (BUG-010): Launcher / Bridge 真实进程存活与端口状态机自愈"]
    end

    subgraph 第二阶段：架构解耦与工程化治理 (P1)
        T19["🎯 任务 19 (PERF-005): App.tsx 1.13 万行真·路由文件拆分与 lazy chunk 编译"]
        T20["🎯 任务 20 (PERF-006): renderer-inject.js 1.05 万行单 IIFE 模块工程化打包"]
    end

    subgraph 第三阶段：数据性能、协议深度兼容与观测 (P2)
        T21["🎯 任务 21 (PERF-007): SQLite 多 Schema 真实接入与 Keyset 游标后端闭环"]
        T22["🎯 任务 22 (BUG-011): 国产大模型协议 / 多模态 / Computer Use / WeChat 协议枚举深度契约"]
        T23["🎯 任务 23 (TEST-001): 跨平台最小自动化 E2E 回归与 CI 强守护"]
    end

    V1 --> T16
    T16 --> T17
    T17 --> T18
    T18 --> T19
    T19 --> T20
    T20 --> T21
    T21 --> T22
    T22 --> T23
```

---

## 📝 详细任务定义与实施计划

### 【任务 16 (P0 / BUG-008)】供应商切换后端跨层事务与原子快照撤销

- **🎯 阶段计划 (Plan)**：
  - **核心痛点**：第一阶段任务 14 实现了前端差异预检与 React 内存级回滚，但底层 `config.toml` 与 `auth.json` 仍为单向直接覆盖，缺乏后端拥有的不可伪造事务快照（Transaction Handle）。一旦切换中途发生磁盘异常或进程退出，容易遗留半同步的损坏配置；且点击“一键撤销”若后端恢复失败，前端仍可能错误报告成功。
  - **实施方案**：
    1. 在 `crates/codex-plus-core/src/relay_switch.rs` 中抽象不可变事务实体 `SwitchTransactionSnapshot`，切换前以原子方式将当前 live 目录的 `config.toml`、`auth.json` 与 `settings.json` 整体捕获并计算 SHA-256；
    2. 提供带有防重放 Token 的后端回滚接口 `restore_switch_transaction(token)`，仅在文件系统和校验和均严格匹配时执行无损倒回；
    3. 前端调用失败时阻断状态重置，保留撤销令牌并展示真实后端失败原因。
  - **验收标准**：
    - 模拟文件写入失败场景，系统能够 100% 原子复原配置文件，不产生脏数据；
    - 撤销接口在后端执行失败时，前端坚决不显示“撤销成功”；
    - 敏感字段（API Key、Token）在事务快照中脱敏遮蔽或安全隔离，不落入明文排错日志。

---

### 【任务 17 (P0 / BUG-009)】环境变量值级归属判定与 401 凭证防破坏

- **🎯 阶段计划 (Plan)**：
  - **核心痛点**：目前 `env_conflicts.rs` 粗暴按 `OPENAI_` 前缀判定环境变量冲突并提示清理，容易误删用户系统级别合法设置的其他工具 key；且当系统存在环境变量但 `auth.json` 未同步时，容易导致官方客户端启动后出现隐蔽的 401 Unauthorized。
  - **实施方案**：
    1. 建立基于“变量名 + 来源 + 真实值哈希 + 目标 profile”的多维归属判定模型；
    2. 当系统环境变量的值与当前选定 profile 预期值完全一致时，自动判定为“一致无冲突”，消除误报；
    3. 只有当环境变量真实属于历史冲突供应商时才触发清理推荐，并提供一键安全隔离，确保清理后绝不丢失原始密钥。
  - **验收标准**：
    - 针对相同密钥不再反复弹出冲突误报；
    - 4 种供应商模式（官方登录/混入/纯API/聚合）切换后，启动客户端能 100% 正常鉴权，不再出现无 Authorization 头导致的 401 故障；
    - 单元测试覆盖环境变量往返一致性与值级保护。

---

### 【任务 18 (P0 / BUG-010)】Launcher / Bridge 真实进程存活与端口状态机自愈

- **🎯 阶段计划 (Plan)**：
  - **核心痛点**：Watchdog 轮询目前仍存在对进程异常退出判定不够精确的问题；当 Electron 崩溃或外部强行杀死 Codex 时，若 CDP 端口消失或 Bridge stale，容易引发生成重试风暴。
  - **实施方案**：
    1. 细化守护状态机：`starting` -> `ready` -> `degraded` -> `stopping` -> `stopped` -> `needs_user_action`；
    2. 精确区分“进程已退出”、“CDP 端口不可达”、“页面忙/正在流式”、“Bridge 离线”四类故障，为每种故障设定独立判定逻辑与有限重试退避；
    3. 进程完全终止后，Watchdog 在固定窗口内主动停机释放所有资源，杜绝空转。
  - **验收标准**：
    - 连续 100 次合成启动/终止/重启测试，57321 端口绑定成功率 100%，无残留孤儿进程；
    - 官方应用被强杀时，守护进程在 2 秒内识别并进入优雅停机，不陷入死循环轮询。

---

### 【任务 19 (P1 / PERF-005)】App.tsx 1.13 万行真·路由文件拆分与 lazy chunk 编译

- **🎯 阶段计划 (Plan)**：
  - **核心痛点**：第一阶段任务 10 虽已完成全量组件的 `React.memo` 解耦与 Suspense 占位，但所有 13 个 Screen 仍集中写在单一 `App.tsx` 文件中（1.17 万行），使得 Vite 编译时的单个 bundle 过大，且源码极难协同维护。
  - **实施方案**：
    1. 在 `apps/codex-plus-manager/src/views/` 目录下将 13 大视图物理拆分为独立 `.tsx` 文件（如 `views/RelayScreen.tsx`、`views/SessionsScreen.tsx` 等）；
    2. 采用标准顶层动态导入 `const RelayScreen = lazy(() => import("./views/RelayScreen"))`，实现真正的代码分割与按需 Chunk 加载；
    3. 将 `App.tsx` 精简为纯粹的顶层路由、侧边栏容器与全局状态协调器（控制在 800 行以内）。
  - **验收标准**：
    - 首屏打开只加载必要的基础 Shell 与首页 Chunk，非当前页面视图按需加载；
    - 拆分后所有单测 187+ 项保持 100% 绿灯通过，UI 行为与状态完全一致。

---

### 【任务 20 (P1 / PERF-006)】renderer-inject.js 1.05 万行单 IIFE 模块工程化打包

- **🎯 阶段计划 (Plan)**：
  - **核心痛点**：`assets/inject/renderer-inject.js` 目前为一个超过 10,500 行的原生 JavaScript 单大文件，包含 IIFE 混写，缺乏现代 TypeScript 类型约束与构建流支持，修改极易引入隐藏语法或作用域冲突。
  - **实施方案**：
    1. 建立模块化构建结构：按生命周期、选择器适配器、会话清理、Markdown 导出、外观皮肤、Bridge 通信等拆解为小模块；
    2. 利用 Vite/Rollup 构建输出单一且极致轻量的目标 bundle，保持对 Rust `assets.rs` 注入载入的 100% 透明兼容；
    3. 增加关键函数的静态单元测试。
  - **验收标准**：
    - 构建产物与原功能 100% 等价，保持纯本地运行与零外联；
    - 消除全局变量污染，注入执行更加健壮。

---

### 【任务 21 (P2 / PERF-007)】SQLite 多 Schema 真实接入与 Keyset 游标后端闭环

- **🎯 阶段计划 (Plan)**：
  - **核心痛点**：第一阶段任务 13 建立了高效的 SQLite 预排序覆盖索引与前端 Keyset 检索辅助逻辑，但前端目前仍主要使用传统的 `offset` 查询，未完全把 Keyset 游标穿透到后端完整分页链路中。
  - **实施方案**：
    1. 将后端 `list_local_sessions_keyset` 指令与前端滚动/翻页全面对接；
    2. 兼容各种不同 Codex 官方版本生成的复杂 SQLite Schema（含只读数据库安全退避）；
    3. 保证海量会话检索下不仅查询快，且在新增/删除会话时分页数据绝无抖动或跳页。
  - **验收标准**：
    - 在万级会话规模下，下一页加载耗时稳定保持在 1ms 级别；
    - 翻页遍历中新增或删除会话不会出现记录重复或漏查。

---

### 【任务 22 (P2 / BUG-011)】国产大模型协议 / 多模态 / Computer Use / WeChat 深度契约

- **🎯 阶段计划 (Plan)**：
  - **核心痛点**：各家大模型 API 行为差异较大（如部分网关不支持 `custom` tool、部分国产模型图片格式要求苛刻、WeChat sandbox 枚举不一致、思考链 `<think>` 标签处理方式不同等）。
  - **实施方案**：
    1. 扩展 `ModelCapability` 矩阵，支持细粒度的模型特性声明与适配；
    2. 对不支持 custom tool 的模型实现标准 `function` 转换或安全降级引导；
    3. 修复微信机器人调 app-server 传参时的 sandbox 权限枚举转换。
  - **验收标准**：
    - 多种非标 API 场景下均能稳定输出流式内容，不崩溃、不报错；
    - 单元测试覆盖各类异常报文与边界格式。

---

### 【任务 23 (P2 / TEST-001)】跨平台最小自动化 E2E 回归与 CI 强守护

- **🎯 阶段计划 (Plan)**：
  - **实施方案**：
    1. 建立模拟 Electron 与 CDP 通信的真实自动化回归测试管线；
    2. 覆盖启动、热重启、页面刷新、断线重连、注入生效的全生命周期；
    3. 在 GitHub Actions 中建立完备的 PR 质量门禁，杜绝任何未经自动化测试的隐形退化进入生产分支。
  - **验收标准**：
    - CI 全自动化跑通全部端到端回归流程；
    - 任何导致端口泄漏或凭据泄露的改动均被门禁自动阻断。

---

## 📅 阶段执行节奏

- **第一阶段 (重点攻坚)**：任务 16 -> 任务 17 -> 任务 18（彻底筑牢系统稳定性底座）；
- **第二阶段 (工程解耦)**：任务 19 -> 任务 20（大幅改善代码健康度与加载开销）；
- **第三阶段 (性能拓展)**：任务 21 -> 任务 22 -> 任务 23（全面提升海量数据体验与生态兼容）。
