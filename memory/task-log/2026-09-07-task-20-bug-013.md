# 2026-09-07 Task 20 / BUG-013 实施记录

## 目标与边界

- Codex session window: `01a07a59-340b-7821-bfa0-19b1da5f781c`。
- 基线：分支 `Gemini`，HEAD `82d0bd6`，前端测试基线 198 项。
- 目标：让注入端会话分享彻底保持本地 Markdown 导出与剪贴板复制，移除残留公网分享请求、伪链接和无效加密载荷。
- 边界：只修改注入脚本、对应契约测试和 `DEVELOPMENT_PLAN_2.md`；不修改后端公网导入 API；保留原有本地会话导入契约；不提交、不推送。

## 调研与决策

- 经验记录命中：项目 memory 目录没有 `experience.md`，复用 Task 19 的证据分级和本地隐私边界记录。
- 在线方案调研：MDN 的 Clipboard `writeText()` 是成熟的系统剪贴板路径；marka.md 作为本地优先 Markdown 工具采用“复制前不离开本机”的交互，仓库显示 473 stars、344 commits。结论是复用现有剪贴板与 DOM Markdown 结构，不引入分享服务或新依赖。
- 根因：注入脚本已经复制本地 Markdown，但仍调用 `/share/create`、生成公网基准 URL、保留 AES 远程载荷和旧公网 origin 白名单。
- 决策：保留本地 `/session/export`，失败时回退 DOM；若本地返回 Markdown 字段则优先使用，否则复制 DOM Markdown。导入消息仅接受当前窗口与当前 origin。
- 冲突裁决：用户本次任务的“纯本地导出”边界优先于历史远程导入白名单；保留本地会话导入函数与 `/session/import`，仅去掉公网来源。

## 实施结果

- `assets/inject/renderer-inject.js`：移除 `/share/create`、公网分享常量、AES 远程载荷、`offlineShareRef` 与 Discord/Telegram 外链死分支；保留本地 Markdown 与剪贴板 fallback。
- `apps/codex-plus-manager/src/renderer-inject.test.ts`：远程分享断言改为禁止性断言，并增加本地 Markdown、clipboard、本地 origin、无公网域名契约。
- `DEVELOPMENT_PLAN_2.md`：登记 Task 20 的 Actual Steps 与 Results & Verification。

## 验证与限制

- `npm test`：**198/198 passed, 0 failed**。
- `node --test src/renderer-inject.test.ts`：**36/36 passed**。
- `git diff --check`：通过；仅有 Git 的 LF/CRLF 提示。
- 注入脚本静态扫描：未发现 `share.codexpp.cc`、`codexpp-share.pages.dev`、`/share/create`、`offlineShareRef`、Discord/Telegram 外链。
- 未运行 TypeScript 类型检查、Rust 编译或桌面 E2E；本任务未触碰后端公网分享导入 API。
- 没有输出真实凭证；没有提交或推送。
