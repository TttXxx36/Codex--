# 任务 21 (P1 / TEST-001) 任务记录

- 日期：2026-09-07
- 平台会话：`01a07b23-a970-73a2-80dc-791307e44995`
- 基线：`Gemini@6642e1a5a063b40c2946a5512f195920eeb3b275`
- 产出 commit：无（按请求保留现场，未提交、未推送）

## 理解与边界

建立跨平台最小外环 CI，并增强本地纯 Node.js smoke/脱敏门禁，为任务 22/23 的重构提供可阻断回归的质量入口。只修改任务 21 所需的 workflow、测试 fixture、门禁测试、大纲和本任务日志；不修改发布 workflow、不接触凭据、不执行外部发布。

## 验收标准

1. `ci.yml` 触发分支、两个核心 job、Node/Rust 工具链、锁文件安装、缓存、测试/类型/构建命令齐全且权限最小。
2. 新门禁被 `npm test` 的 `src/*.test.ts` 自动发现，扫描源码及存在的前端产物，不接受 Bearer、`sk-` 或凭据赋值形状。
3. 本地 `npm test` 零失败；文档登记真实结果与未验证边界。

## 决策与实施

- 复用仓库已有的 `actions/checkout@v4`、`actions/setup-node@v4`、`actions/cache@v4` 与 `dtolnay/rust-toolchain@stable` 约定，Node 采用 22，Rust job 采用 Windows runner 以覆盖 Windows 编译环境。
- 前端先 `vite:build` 再 `npm test`，使 CI 中已生成的 `dist` 同时进入脱敏扫描；本地没有 build 依赖时门禁仍扫描 `src`。
- 现有脱敏测试只使用合成 fixture；为满足静态扫描，将其从源码中的连续 key/token 改为运行时片段拼接。该决策保持测试语义，不保存真实凭据。
- 冲突仲裁：用户任务清单要求新增独立 `ci.yml`，仓库已有 `pr-build.yml` 的发布/产物职责保留不动；遵循用户范围，只新增最小门禁而不重写既有发布工作流。

## 结果与证据

- `npm test`：**199/199 passed，0 failed**。
- 定向新增/相关测试：**36/36 passed**。
- `git diff --check`：通过。
- `npm run check`：未通过/未验证，当前 manager 依赖目录没有 `tsc` 可执行文件。
- `npm run vite:build`：未通过/未验证，当前 manager 依赖目录没有 `vite` 可执行文件。
- Rust 与真实 Windows CI：未验证，本机没有 `cargo` / `rustc`，且未推送触发远程 workflow。
- actionlint/YAML 专用解析器：本机不可用；已完成 workflow 结构、action 引用、分支过滤、脚本和锁文件路径的人工/静态复核，未把它升级为云端通过证据。
